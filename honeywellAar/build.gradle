plugins {
    id 'com.android.library'
}

android {
    namespace 'com.yourorg.honeywellwrapper'
    compileSdk 34
    defaultConfig { minSdk 24 }
}

dependencies {
    // Loads DataCollection.aar from libs
    implementation files('libs/DataCollection.aar')
}
